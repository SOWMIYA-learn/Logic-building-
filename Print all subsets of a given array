

from itertools import combinations

arr = [1, 2, 3]
subsets = [[]] + [list(c) for i in range(1, len(arr)+1) for c in combinations(arr, i)]
print(subsets)

O/P :
[[], [1], [2], [3], [1, 2], [1, 3], [2, 3], [1, 2, 3]]

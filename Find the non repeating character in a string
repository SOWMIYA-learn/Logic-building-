

def first_non_repeating(s):
    for c in s:
        if s.count(c) == 1:
            return c
    return None

string = "swiss"
result = first_non_repeating(string)
print("First non-repeating character:", result)

O/P :

For the input string = "swiss", the output will be:
First non-repeating character: w
